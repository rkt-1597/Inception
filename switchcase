#include <stdio.h>

int main() {
    char operator;    // Variable to store the operator
    double num1, num2, result;  // Variables to store the two operands and the result

    // Prompt the user to enter an operator
    printf("Enter an operator (+, -, *, /): ");
    scanf(" %c", &operator); // Note the space before %c to ignore any leading whitespace

    // Prompt the user to enter two operands
    printf("Enter two operands: ");
    scanf("%lf %lf", &num1, &num2); // Read two double values

    // switch case
    switch (operator) {
         // Case for addition
           
         // Case for subtraction
           
         // Case for multiplication
           
         // Case for division
            
        default: // Case for invalid operator
    }

    return 0; 
}
