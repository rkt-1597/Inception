#include <stdio.h>

/*
 * Calculate Average of an Array
 * -----------------------------
 * How to do it:
 * 1. Loop through all the elements of the array using a `for` loop.
 * 2. Inside the loop, sum all the elements of the array.
 * 3. After the loop, divide the sum by the total number of elements to get the average.
 * 4. Return or print the average.
 */

int main() {
    // Declare and initialize an array
    
    // Calculate the average
        // Initialize sum variable
        // Use for loop to sum all the elements
    
    // Calculate average

    // Print the average
    
    // Print the average with two decimal points

    return 0;
}

