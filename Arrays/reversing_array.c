#include <stdio.h>

/*
 * Reverse an Array
 * ----------------
 * How to do it:
 * 1. Loop through half of the array elements (from the start to the middle). Use a for loop.
 * 2. Inside the loop, swap each element with the corresponding element from the end.
 * 3. Use a temporary variable to hold one element during the swap.
 * 4. Make sure to correctly index the array for swapping.
 */

int main() {
    // Declare and initialize an array

    // Reverse the array directly in main. Use for loop.
        // Swap elements
        
    // Print the reversed array

    return 0;
}
